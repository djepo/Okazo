{%if parameters['showHeader'] or parameters['showLanguageChooser'] or parameters['showLogin']%}
<nav class="navbar navbar-default navbar-flat" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle navbar-btn-right" data-toggle="collapse" data-target="#navbar-collapse-userlng">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {#
            <button type="button" class="navbar-toggle navbar-btn-left" data-toggle="collapse" data-target="#navbar-collapse-search">
                <span class="sr-only">Toggle navigation</span>
                <span class="glyphicon glyphicon-search"></span>                
            </button>
            #}            
            <a class="hidden-lg hidden-md navbar-brand" href="{{path('homepage')}}">
                Okazo
            </a>
            
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbar-collapse-userlng">
            {% include 'okazoMainBundle:blocks:languageChooser.html.twig' %}
            {% include 'okazoUserBundle:blocks:userMenu.html.twig' %}
        </div><!-- /.navbar-collapse -->
         {# TEST DE FORMULAIRE DE RECHERCHE INTEGRE A NAVBAR SUR COLLAPSE UNIQUEMENT #}
        {#
        <div class="collapse navbar-collapse" id="navbar-collapse-search">            
            <form class="navbar-form navbar-left" role="search">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Search">
                </div>
                <button type="submit" class="btn btn-default">Submit</button>
            </form>
            {% include 'okazoMainBundle:blocks:languageChooser.html.twig' %}
            {% include 'okazoUserBundle:blocks:userMenu.html.twig' %}
        </div><!-- /.navbar-collapse -->
        #}
    </div>
</nav>
{%endif%}